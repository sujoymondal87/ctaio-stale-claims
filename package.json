{
  "name": "ctaio-stale-claims",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "node scripts/check.mjs",
    "check": "node scripts/check.mjs --strict"
  }
}
